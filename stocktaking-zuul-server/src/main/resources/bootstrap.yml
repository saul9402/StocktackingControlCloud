eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
spring:
  application:
    name: stocktaking-zuul-server
  cloud:
    config:
      uri:
      - http://localhost:8888
      label: master
  profiles:
    active:
    - dev
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
